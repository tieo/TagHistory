appId: io.github.tieo.taghistory
name: History — tapping a list item selects the point (highlighted entry)
---
- runFlow:
    file: 00_seed_test_data.yaml

- tapOn:
    id: "btn_card_history"
- extendedWaitUntil:
    visible:
      text: "Today"
    timeout: 15000

# Expand sheet
- swipe:
    start: "50%, 88%"
    end: "50%, 40%"
    duration: 400
- extendedWaitUntil:
    visible:
      id: "history_item_0"
    timeout: 15000

# Item 0 (newest) is pre-selected. Tap item 1 to select it.
- tapOn:
    id: "history_item_1"
- extendedWaitUntil:
    visible:
      id: "history_item_1"
    timeout: 15000

# Sheet remains open after tap (item 1 still visible).
- assertVisible:
    id: "history_item_1"
- assertVisible:
    id: "history_item_0"
- assertVisible:
    text: ".*data point.*"

# Tap item 2 to select older point. Day label is still Today (3 points).
- tapOn:
    id: "history_item_2"
- extendedWaitUntil:
    visible:
      id: "history_item_2"
    timeout: 15000
- assertVisible:
    text: "Today"

# Tap back to item 0 — selection bounces around without crashing.
- tapOn:
    id: "history_item_0"
- extendedWaitUntil:
    visible:
      id: "history_item_0"
    timeout: 15000
- assertVisible:
    id: "history_item_0"

# At least one timestamp line is rendered — catches blank labels.
- assertVisible:
    text: ".*At \\d.*"

# Day-prev to Yesterday — items reset, item 0 is now Yesterday's newest.
- tapOn:
    id: "btn_day_prev"
    optional: true
- extendedWaitUntil:
    visible:
      text: "Yesterday"
    timeout: 15000
- extendedWaitUntil:
    visible:
      id: "history_item_0"
    timeout: 15000
- tapOn:
    id: "history_item_0"
- extendedWaitUntil:
    visible:
      id: "history_item_0"
    timeout: 15000

- back
- extendedWaitUntil:
    visible:
      id: "btn_settings"
    timeout: 15000
- assertVisible:
    text: "Car Keys"
- assertNotVisible:
    text: "Yesterday"
