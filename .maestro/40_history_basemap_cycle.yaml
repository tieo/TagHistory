appId: io.github.tieo.taghistory
name: History — basemap cycle button cycles through light/dark/satellite
---
# BasemapCycleButton has contentDescription "Basemap: <state>". Cycling
# advances LIGHT -> DARK -> SATELLITE -> LIGHT.
- runFlow:
    file: 00_seed_test_data.yaml

- tapOn:
    id: "btn_card_history"
- extendedWaitUntil:
    visible:
      id: "btn_day_prev"
    timeout: 5000

# Find the button (one of the three states). Tap it to cycle. We don't
# assume the starting state because it follows the system theme.
- assertVisible:
    text: "Basemap: .*"
- tapOn:
    text: "Basemap: .*"
- assertVisible:
    text: "Basemap: .*"
- tapOn:
    text: "Basemap: .*"
- assertVisible:
    text: "Basemap: .*"
- tapOn:
    text: "Basemap: .*"

# After three cycles we should be back at the original. Verify map screen
# is still alive and day controls are still rendering.
- assertVisible:
    id: "btn_day_prev"
- assertVisible:
    id: "btn_day_next"
- assertVisible:
    text: "Today"

# Cycle three more times — full second loop. Day controls remain alive.
- tapOn:
    text: "Basemap: .*"
- tapOn:
    text: "Basemap: .*"
- tapOn:
    text: "Basemap: .*"
- assertVisible:
    id: "btn_day_prev"
- assertVisible:
    text: "Today"

# Day-prev → Yesterday: cycling basemap doesn't break day nav state.
- tapOn:
    id: "btn_day_prev"
- extendedWaitUntil:
    visible:
      text: "Yesterday"
    timeout: 15000
- tapOn:
    text: "Basemap: .*"
- assertVisible:
    text: "Yesterday"
- tapOn:
    id: "btn_day_next"
- extendedWaitUntil:
    visible:
      text: "Today"
    timeout: 15000

- back
- extendedWaitUntil:
    visible:
      id: "btn_settings"
    timeout: 5000
- assertVisible:
    text: "Car Keys"
